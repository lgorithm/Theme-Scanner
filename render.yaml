services:
  - type: web
    name: themescanner-backend
    env: docker
    plan: free
    dockerfilePath: ./backend/Dockerfile
    envVars:
      - key: OPENAI_API_KEY
        value: your-openai-key-here
    disk:
      name: chroma-data
      mountPath: /data
      sizeGB: 1